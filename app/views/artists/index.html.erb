<h1>Artists</h1>

<ul>
  <% @artists.each do |artist| %>
    <li>
      <%= link_to artist.name, artist_path(artist), class: "artist_name" %>
    </li>
  <% end %>
</ul>


<p><%= link_to "Create New Artist!", new_artist_path %></p>
<div class="footer">
  <p><%= link_to "THROB", root_path %></p>
</div>